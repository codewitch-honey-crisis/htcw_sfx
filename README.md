# SFX

SFX is a cross platform MIDI and audio library. It does produce output on its own, since doing so requires hardware specific calls.

Instead it provides foundation classes for manipulating audio and MIDI data.

Currently, this is a MIDI library, with other audio coming.